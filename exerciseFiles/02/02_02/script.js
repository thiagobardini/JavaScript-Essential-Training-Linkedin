var negInteger = -3.14159265359; // typeof -> number
var escQuote = "Quotes can also be \"escaped\"."; // typeof -> string
var theSunIsWarm = true; // typeof -> Boolean
var emptyInside = null; // typeof -> null 
var justAnotherVariable; // typeof -> undefined

// Try this in your console:
// console.log(typeof insertVariableName);

// se quiser incluir "" quotation mark na string use com o exemplo abaixo \"newData\" 
var t = "thiago \"bardini\".";
console.log(t);